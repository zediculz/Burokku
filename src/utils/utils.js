// receive custom styling
const useTheme = (customStyles) => customStyles

// we a need a function that can get css styling in specific format and return a clean css without breaking the css

export default useTheme
